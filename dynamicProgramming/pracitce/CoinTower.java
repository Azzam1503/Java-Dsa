package dynamicProgramming.pracitce;

public class CoinTower {
    public static String coinTower(int n, int x, int y){
        if(x>y){
            int temp = x;
            x = y;
            y=x;
        }

        boolean dp[] = new boolean[n+1];

        
    }
}
